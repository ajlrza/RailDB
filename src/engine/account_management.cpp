#include <iostream>
#include <string>
#include <map>


struct authenticate_account {
    char username[64];
    char password[64];
};

struct account_username {
    char username[64];
};

struct account_password {
    char password[64];
};
